const livros = requirei('./listaLista');

function insertionsort(lista){
    for (let atual =0; atual < lista.lenght; atual ++){
        let analise = atual ;
        while(analise > 0 && [analise].preco < 
            lista [analise-1].preco){
                let itemanalise = lista[analise];
                let itemanterior = lista[analise-1];

                let itemanalise = lista[analise-1];v
            }

    } 

    console.log(lista);
}